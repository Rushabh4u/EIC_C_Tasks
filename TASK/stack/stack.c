#include<stdio.h>
#include"stack.h"

bool is_empty(int top){
	if (top == -1)
		return TRUE;
	retun FALSE;
}

bool is_full(int top){

}
void push(int s[], int data);
void pop(int s[]);
void clear(int s[]);
int top(int s[]);
int top_and_pop(int s[]);
